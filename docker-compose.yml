version: "3.9"

services:
  # ---------------------------------------------------------------
  # Python routing backend (OGRouting engine + RouteIQ JSON bridge)
  # ---------------------------------------------------------------
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    ports:
      - "8000:8000"
    environment:
      # Auth for the legacy OGRouting web UI (set to your preference)
      FR_AUTH_ENABLED: "false"
      FR_LOG_LEVEL: "INFO"
      # Allow the Next.js dev server and Vercel preview URLs to call the backend
      ROUTEIQ_ALLOWED_ORIGINS: "http://localhost:3000,https://*.vercel.app"
      # FieldRoutes credentials (copy from your .env.local)
      FIELDROUTES_BASE_URL: ${FIELDROUTES_BASE_URL:-https://api.fieldroutes.com}
      FIELDROUTES_AUTH_KEY: ${FIELDROUTES_AUTH_KEY:-}
      FIELDROUTES_SECRET_KEY: ${FIELDROUTES_SECRET_KEY:-}
      # OSRM — leave blank to use the public demo server (rate-limited)
      # Set to your self-hosted instance for production
      OSRM_BASE_URL: ${OSRM_BASE_URL:-http://router.project-osrm.org}
    volumes:
      - backend_data:/app/data
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/healthz"]
      interval: 30s
      timeout: 10s
      retries: 3
    restart: unless-stopped

  # ---------------------------------------------------------------
  # Next.js frontend
  # ---------------------------------------------------------------
  frontend:
    build:
      context: .
      dockerfile: Dockerfile.frontend
    ports:
      - "3000:3000"
    environment:
      # Backend URL — inside Docker, use the service name
      NEXT_PUBLIC_BACKEND_URL: "http://backend:8000"
      # Firebase (copy from your Vercel env vars)
      NEXT_PUBLIC_FIREBASE_API_KEY: ${NEXT_PUBLIC_FIREBASE_API_KEY:-}
      NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN: ${NEXT_PUBLIC_FIREBASE_AUTH_DOMAIN:-}
      NEXT_PUBLIC_FIREBASE_PROJECT_ID: ${NEXT_PUBLIC_FIREBASE_PROJECT_ID:-}
      NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET: ${NEXT_PUBLIC_FIREBASE_STORAGE_BUCKET:-}
      NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID: ${NEXT_PUBLIC_FIREBASE_MESSAGING_SENDER_ID:-}
      NEXT_PUBLIC_FIREBASE_APP_ID: ${NEXT_PUBLIC_FIREBASE_APP_ID:-}
      FIREBASE_SERVICE_ACCOUNT: ${FIREBASE_SERVICE_ACCOUNT:-}
      NEXT_PUBLIC_GOOGLE_MAPS_KEY: ${NEXT_PUBLIC_GOOGLE_MAPS_KEY:-}
    depends_on:
      backend:
        condition: service_healthy
    restart: unless-stopped

volumes:
  backend_data:
